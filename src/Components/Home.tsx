import Button from "./Button";

const Home = () => {
  return (
    <div className="min-h-screen flex justify-center items-center">
      <Button backgroundColor="red" pillShaped={true} fontSize={20} />
    </div>
  );
};

export default Home;
